import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  //Strict mode highlights potential problems in App.

  document.getElementById("root")
);
